/**
 * Stub for react-native-razorpay.
 * react-native-razorpay requires native linking (dev build / EAS build).
 * This stub satisfies Metro's module resolution so the app bundles in
 * Expo Go. The RazorpayCheckout helper detects the missing `open` function
 * via isNativeRazorpayAvailable() and falls back to a WebView flow.
 */
"use strict";

const RazorpayCheckout = {
  open: undefined,
};

module.exports = RazorpayCheckout;
module.exports.default = RazorpayCheckout;
